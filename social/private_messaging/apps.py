from django.apps import AppConfig

class PrivateMessagingConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "social.private_messaging"
    verbose_name = "Mensajería Privada"
